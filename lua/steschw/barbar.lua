local barbar = require("bufferline")

barbar.setup({
    animation = false,
    auto_hide = false,  --[[ Do not hide if there is only a single buffer ]]--
    tabpages = false,
    closeable = false,
    clickable = false,
    icons = "both",
})
